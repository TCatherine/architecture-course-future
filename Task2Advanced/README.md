# Task 2. CI/CD + удалённое хранение состояния Terraform


Каждое окружение хранит state в одном бакете `tki-terraform-state`, но под своим ключом — окружения полностью изолированы

CD-пайплайн написан для Github Actions и применен в репозитории, с которого сдается работа. Пайплайн запускается на push в ветку `main` и `solution`.
